version: "3.8"
services:
  mariadb:
    image: mariadb:11
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: example-root-pw
      MARIADB_DATABASE: briva_diena
      MARIADB_USER: briva_diena_user
      MARIADB_PASSWORD: briva_diena_password
    ports:
      - "3307:3306"
    volumes:
      - mariadb_data:/var/lib/mysql

volumes:
  mariadb_data: